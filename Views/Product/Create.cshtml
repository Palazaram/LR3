@model NewWebShopApp.Models.Product

@{
    
}
<h2>Додавання продукту</h2>

<div class="container">
    <form asp-action="Create" asp-controller="Product" method="post" enctype="multipart/form-data">
        <p>
            @Html.LabelFor(m => m.name, "Назва")
            @Html.EditorFor(m => m.name)
            <span asp-validation-for="name"></span>
        </p>
        <p>
            @Html.LabelFor(m => m.img, "Фото")
            <input type="file" name="uploadedFile" />
            <span asp-validation-for="img"></span>
        </p>
        <p>
            @Html.LabelFor(m => m.price, "Ціна")
            @Html.EditorFor(m => m.price)
            <span asp-validation-for="price"></span>
        </p>
        <p>
            @Html.LabelFor(m => m.box, "Упаковка")
            @Html.EditorFor(m => m.box)
            <span asp-validation-for="box"></span>
        </p>
    
        <p>
            @Html.LabelFor(m => m.CategoryId, "Категорія")
            @Html.DropDownListFor(m => m.CategoryId, ViewBag.Categories as IEnumerable<SelectListItem>)
        </p>
        <p>
            <input type="submit" value="Save" />
        </p>
    </form>
</div>

        